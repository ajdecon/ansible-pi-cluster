---
- hosts: headnode
  user: pi
  tasks:
  - name: download this repo
    action: "git repo=https://bitbucket.org/ajdecon/ansible-pi-cluster.git dest=/home/pi/ansible-pi-cluster"
  - name: git pull
    action: "shell cd /home/pi/ansible-pi-cluster && git pull"
